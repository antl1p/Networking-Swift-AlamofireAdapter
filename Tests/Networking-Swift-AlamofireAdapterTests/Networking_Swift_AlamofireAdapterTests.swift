import XCTest
@testable import Networking_Swift_AlamofireAdapter

final class Networking_Swift_AlamofireAdapterTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Networking_Swift_AlamofireAdapter().text, "Hello, World!")
    }
}
